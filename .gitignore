# Node
node_modules/
npm-debug.log
package-lock.json

# Clasp
.clasprc.json
# Generated at the repo root by `npm run env:staging|production`,
# copied from deployment/clasp.{staging,production}.json
.clasp.json

# Local config (contains sensitive URLs)
frontend/dashboard/config.local.json

# Environment files with admin tokens (NEVER COMMIT)
.env.staging
.env.production
.env

# OS / editor cruft
.DS_Store
*.swp
*.swo

# Scratch space — not part of the project
/tmp/
